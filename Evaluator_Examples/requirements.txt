# Agent-Evaluator 예제 의존성
# Python 3.8+
#
# 프로젝트 루트에서 설치:
#   pip install -e ".[dev]"        # 개발 환경 (권장)
#   pip install -e ".[all]"        # crewai/autogen/otel 제외 전체
#   pip install -e ".[full]"       # 전체 (⚠️ 10분+ 소요)
#
# PyPI에서 설치:
#   pip install "agent-evaluator[all]"

# ─────────────────────────────────────────────────────────────────────────────
# 예제 01 — Layer 1 전체 지표 (Accuracy · Hallucination · Quality · Latency · Token · TCR)
# 예제 02 — Layer 2 전체 지표 (ToolCall · Retry · Coordination · Workflow · Security · 대화)
# 예제 03 — 프레임워크 어댑터 (LangChain · LangGraph · CrewAI · AutoGen + 크로스 파이프라인)
# 예제 04 — 데코레이터 전체 API (@agent_eval · @batch_eval · @conversation_eval · QuickEval)
# 예제 05 — 실시간 (StreamingEvaluator · ImplicitFeedback · AlertEngine · SimpleTaskAlertRule)
# 예제 06 — 운영 인프라 (AnomalyDetector · CostTracker · GoldenSetBuilder · evaluation_session)
#
# 위 예제들은 외부 의존성 없음 — agent-evaluator 코어만 필요.
# ─────────────────────────────────────────────────────────────────────────────

numpy>=1.20.0,<3.0.0
pandas>=1.3.0,<4.0.0
python-dotenv>=0.19.0,<2.0.0

# ─────────────────────────────────────────────────────────────────────────────
# 예제 07 — Phoenix OTEL + DeepEval/Ragas (Layer 3 외부 통합)
# ─────────────────────────────────────────────────────────────────────────────
# Phoenix 실시간 모니터링 (OTLP 스팬 송신):
#   pip install "agent-evaluator[otel]"
#   agent-eval monitor            # Phoenix 서버 기동 (기본 포트 6006)
#
# DeepEval / Ragas 외부 평가 (대시보드 '외부평가' 탭 활성화):
#   pip install "agent-evaluator[eval]"
#   OPENAI_API_KEY 필요
#
# LLM Judge 채점 (ground_truth 없이 5차원 자동 채점):
#   pip install "agent-evaluator[llm]"   # OpenAI 또는 Anthropic 키 필요

# ─────────────────────────────────────────────────────────────────────────────
# 대시보드 서버 (agent-eval dashboard)
# ─────────────────────────────────────────────────────────────────────────────
# pip install "agent-evaluator[serve]"
# agent-eval dashboard                     # 기본 포트 8765
# agent-eval dashboard --port 8080 --watch # 포트 지정 + 파일 변경 자동 갱신
